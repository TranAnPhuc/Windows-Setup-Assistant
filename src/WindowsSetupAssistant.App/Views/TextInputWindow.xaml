<Window x:Class="WindowsSetupAssistant.App.Views.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowsSetupAssistant.App.ViewModels"
        xmlns:loc="clr-namespace:WindowsSetupAssistant.App.Localization"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:TextInputViewModel}"
        Title="{Binding Title}"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">

    <StackPanel Margin="20">
        <TextBlock Text="{Binding Prompt}" Margin="0,0,0,8" />
        <TextBox x:Name="InputBox" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="{loc:Text Ui_BtnCancel}" IsCancel="True" MinWidth="96" />
            <Button Content="{loc:Text Ui_BtnOk}" Click="OnOkClick" IsDefault="True" MinWidth="96" Margin="10,0,0,0"
                    Style="{StaticResource PrimaryButton}" />
        </StackPanel>
    </StackPanel>
</Window>
