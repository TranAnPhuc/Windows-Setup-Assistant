<Window x:Class="WindowsSetupAssistant.App.Views.PackageEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WindowsSetupAssistant.App.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:PackageEditorViewModel}"
        Title="{Binding Title}"
        Width="540"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">

    <StackPanel Margin="20">
        <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleText}" Margin="0,0,0,4" />
        <TextBlock Style="{StaticResource MutedText}" Margin="0,0,0,16"
                   Text="Package Id phải đúng như trên nguồn WinGet. Dùng tab &quot;Tìm trên WinGet&quot; để lấy Id chính xác." />

        <TextBlock Text="Tên phần mềm" Margin="0,0,0,4" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="WinGet Package Id" Margin="0,14,0,4" />
        <TextBox Text="{Binding PackageId, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas" />
        <TextBlock Style="{StaticResource MutedText}" Margin="0,4,0,0"
                   Text="Ví dụ: Google.Chrome, Git.Git, 7zip.7zip, Notepad++.Notepad++" />

        <TextBlock Text="Nhóm phần mềm" Margin="0,14,0,4" />
        <ComboBox ItemsSource="{Binding Categories}"
                  SelectedItem="{Binding Category}"
                  DisplayMemberPath="DisplayName" />

        <TextBlock Text="Ghi chú (không bắt buộc)" Margin="0,14,0,4" />
        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="60"
                 VerticalContentAlignment="Top" />

        <Border Margin="0,14,0,0" Padding="10,8" CornerRadius="6"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource DangerBrush}"
                BorderThickness="1"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{Binding ErrorMessage}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource DangerBrush}" />
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Huỷ" IsCancel="True" MinWidth="96" />
            <Button Content="Lưu" Click="OnSaveClick" IsDefault="True" MinWidth="96" Margin="10,0,0,0"
                    Style="{StaticResource PrimaryButton}" />
        </StackPanel>
    </StackPanel>
</Window>
